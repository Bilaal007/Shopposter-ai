
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shopposter.ai',
  appName: 'ShopPoster AI',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
